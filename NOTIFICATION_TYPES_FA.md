# انواع نوتیفیکیشن در Android

این پروژه از انواع مختلف نوتیفیکیشن‌های Android پشتیبانی می‌کند.

## ✅ مشکلات حل شده

### 1. کاستوم چنل صدا نداشت
- حالا همه کانال‌ها صدای پیش‌فرض Android رو دارن
- می‌تونی صدا رو با `enableSound: true` فعال کنی

### 2. آیکون نوتیفیکیشن نمایش نمیداد
- حالا آیکون اپلیکیشن (large icon) در سمت چپ نمایش داده میشه
- Small icon هم برای status bar تنظیم شده

### 3. نوتیفیکیشن مثل تصویر
- متد جدید `showStyledNotification` اضافه شد
- نوتیفیکیشن با آیکون اپلیکیشن و متن کامل نمایش داده میشه

## 1. نوتیفیکیشن معمولی (Standard Notification)
نوتیفیکیشن‌های معمولی که در status bar نمایش داده می‌شوند.

```dart
await _notificationMaster.showNotification(
  title: 'عنوان',
  message: 'پیام',
  importance: NotificationImportance.high,
);
```

## 2. نوتیفیکیشن استایل‌دار (Styled Notification) ⭐ جدید
این نوتیفیکیشن مثل تصویری که فرستادی نمایش داده میشه - با آیکون اپلیکیشن و متن کامل.

```dart
await _notificationMaster.showStyledNotification(
  title: 'ویرایش عصرگاهی ؟ • now 🔔',
  message: 'این نوتیفیکیشن با آیکون اپلیکیشن و متن کامل نمایش داده میشه',
);
```

**ویژگی‌ها:**
- آیکون اپلیکیشن در سمت چپ
- متن کامل نمایش داده میشه
- صدا و vibration دارد
- زمان نمایش داده میشه (now)

## 3. Heads-Up Notification (نوتیفیکیشن از بالا)
این نوتیفیکیشن از بالای صفحه نمایش داده می‌شود و padding دارد. برای پیام‌های مهم استفاده می‌شود.

```dart
await _notificationMaster.showHeadsUpNotification(
  title: '🔔 پیام مهم',
  message: 'این نوتیفیکیشن از بالای صفحه نمایش داده میشه',
);
```

**ویژگی‌ها:**
- از بالای صفحه ظاهر می‌شود
- Padding دارد
- UI سفارشی
- برای پیام‌های فوری مناسب است

## 4. Full Screen Notification (نوتیفیکیشن تمام صفحه)
این نوتیفیکیشن تمام صفحه را می‌گیرد، مثل تماس ورودی.

```dart
await _notificationMaster.showFullScreenNotification(
  title: '📞 تماس ورودی',
  message: 'این نوتیفیکیشن تمام صفحه رو میگیره',
);
```

**ویژگی‌ها:**
- تمام صفحه را می‌گیرد
- برای موارد بسیار مهم (مثل تماس)
- بیشترین توجه کاربر را جلب می‌کند

## 5. کانال‌های سفارشی (Custom Channels)
می‌توانید کانال‌های سفارشی با تنظیمات خاص بسازید:

```dart
// ساخت کانال سفارشی
await _notificationMaster.createCustomChannel(
  channelId: 'custom_channel_id',
  channelName: 'کانال سفارشی من',
  channelDescription: 'توضیحات کانال',
  importance: NotificationImportance.high,
  enableSound: true,  // ✅ حالا صدا کار می‌کنه
  enableVibration: true,
  enableLights: true,
);

// ارسال نوتیفیکیشن با کانال سفارشی (با آیکون)
await _notificationMaster.showStyledNotification(
  title: 'عنوان',
  message: 'پیام',
  channelId: 'custom_channel_id',
);
```

## 6. Big Text Notification
برای نمایش متن‌های طولانی:

```dart
await _notificationMaster.showBigTextNotification(
  title: 'عنوان',
  message: 'پیام کوتاه',
  bigText: 'متن طولانی که وقتی نوتیفیکیشن باز میشه نمایش داده میشه...',
  importance: NotificationImportance.high,
);
```

## 7. Image Notification
نوتیفیکیشن با تصویر:

```dart
await _notificationMaster.showImageNotification(
  title: 'عنوان',
  message: 'پیام',
  imageUrl: 'https://example.com/image.jpg',
  importance: NotificationImportance.high,
);
```

## 8. Action Notification
نوتیفیکیشن با دکمه‌های عملیاتی:

```dart
await _notificationMaster.showNotificationWithActions(
  title: 'عنوان',
  message: 'پیام',
  actions: [
    {'title': 'پاسخ', 'route': 'reply'},
    {'title': 'بایگانی', 'route': 'archive'},
    {'title': 'حذف', 'route': 'delete'},
  ],
  importance: NotificationImportance.high,
);
```

## تفاوت‌های مهم

### نوتیفیکیشن معمولی vs استایل‌دار
- **معمولی**: بدون آیکون اپلیکیشن، متن ممکنه کوتاه بشه
- **استایل‌دار**: با آیکون اپلیکیشن، متن کامل، زمان نمایش داده میشه ⭐

### استایل‌دار vs Heads-Up
- **استایل‌دار**: فقط در notification bar نمایش داده می‌شود
- **Heads-Up**: از بالای صفحه ظاهر می‌شود و padding دارد

### Heads-Up vs Full Screen
- **Heads-Up**: بخشی از صفحه را می‌گیرد، کاربر می‌تواند آن را نادیده بگیرد
- **Full Screen**: تمام صفحه را می‌گیرد، مثل تماس ورودی

## لاگ‌ها برای دیباگ
همه متدها لاگ می‌زنند:
- 🔧 برای ساخت کانال
- 📱 برای ارسال نوتیفیکیشن
- ✅ برای موفقیت
- ❌ برای خطا

لاگ‌ها را در Logcat با فیلتر `NotificationHelper` یا `CustomNotificationHelper` ببینید.

## نکات مهم

1. **Android 13+**: نیاز به permission دارد
2. **Heads-Up**: فقط با `importance: high` کار می‌کند
3. **Full Screen**: ممکن است در برخی دستگاه‌ها محدودیت داشته باشد
4. **Custom UI**: می‌توانید در `CustomNotificationHelper.kt` تغییرات بدهید
5. **آیکون**: حالا همه نوتیفیکیشن‌ها آیکون اپلیکیشن رو نمایش میدن ✅
6. **صدا**: همه کانال‌ها صدای پیش‌فرض Android رو دارن ✅

## مثال کامل (توصیه شده) ⭐

```dart
// 1. بررسی permission
bool hasPermission = await _notificationMaster.checkNotificationPermission();
if (!hasPermission) {
  await _notificationMaster.requestNotificationPermission();
}

// 2. ساخت کانال سفارشی
await _notificationMaster.createCustomChannel(
  channelId: 'my_channel',
  channelName: 'کانال من',
  importance: NotificationImportance.high,
  enableSound: true,  // صدا فعال
  enableVibration: true,
  enableLights: true,
);

// 3. ارسال نوتیفیکیشن استایل‌دار (مثل تصویر)
await _notificationMaster.showStyledNotification(
  title: 'ویرایش عصرگاهی ؟ • now 🔔',
  message: 'این نوتیفیکیشن با آیکون اپلیکیشن و متن کامل نمایش داده میشه',
  channelId: 'my_channel',
);
```

## سفارشی‌سازی UI

برای تغییر UI نوتیفیکیشن‌های Heads-Up و Full Screen، فایل زیر را ویرایش کنید:
```
android/src/main/kotlin/com/example/notification_master/CustomNotificationHelper.kt
```

می‌توانید:
- رنگ‌ها را تغییر دهید
- الگوی vibration را تغییر دهید
- صدا را تغییر دهید
- Layout سفارشی اضافه کنید (با RemoteViews)

## تغییرات نسخه جدید

### ✅ مشکلات حل شده:
1. **صدا**: همه کانال‌ها حالا صدای پیش‌فرض Android رو دارن
2. **آیکون**: آیکون اپلیکیشن در همه نوتیفیکیشن‌ها نمایش داده میشه
3. **متن کامل**: با `showStyledNotification` متن کامل نمایش داده میشه
4. **لاگ‌های بهتر**: لاگ‌های کامل برای دیباگ اضافه شدن

### 🆕 متدهای جدید:
- `showStyledNotification()`: نوتیفیکیشن با آیکون و متن کامل (مثل تصویر)
- `showHeadsUpNotification()`: نوتیفیکیشن از بالای صفحه
- `showFullScreenNotification()`: نوتیفیکیشن تمام صفحه

